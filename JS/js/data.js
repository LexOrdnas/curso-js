// String, number, undefined, null, boolean, symbol
const nome = 'Alex'; // String
const sobrenome = "Silva"; // String
const nomeCachorro = `Thunder`; // String
const casaNumero = 26; // number
const pesoNumero = 88.52; //number
let nomeAluno; // undefined -> Não aponta pra local nenhum na memória
const sobrenomeAluno = null; // Nulo -> Não aponta pra local nenhum na memória
const aprovado = false; // Booleano -> true, false (Lógico)

console.log(typeof sobrenomeAluno, nomeAluno)

// NaN - Not a number parseInt (inteiro), parseFloat(decimais), Number